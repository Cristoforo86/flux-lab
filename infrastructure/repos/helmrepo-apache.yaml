apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: HelmRepository
metadata:
  name: apache-helm-repo
  namespace: flux-system
spec:
  # URL of your Helm repository
  url: oci://registry-1.docker.io/bitnamicharts
  # Chart name
  chart: apache
  # Helm chart version constraint
  version: 10.2.4
